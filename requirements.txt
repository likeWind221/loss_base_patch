# Infrared Patch Attack (mycode) requirements
# 使用方式：pip install -r requirements.txt
# 提示：请单独安装与您 CUDA 版本匹配的 torch/torchvision（或先安装 CPU 版），再安装其余包。

# Core
numpy>=1.23.5
pillow>=10.0.0
opencv-python>=4.5.0
matplotlib>=3.5.0
scipy>=1.8.0
pandas>=1.3.0
seaborn>=0.11.0
PyYAML>=5.4.1
requests>=2.28.0
tqdm>=4.64.0
psutil>=5.9.0
gitpython>=3.1.30
thop>=0.1.1

# PyTorch（建议先单独安装匹配 CUDA 的版本，再安装其余包）
# 例如：pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=1.8.0
torchvision>=0.9.0

# YOLO/Ultralytics 生态（可选，用于工具链与便捷函数）
ultralytics>=8.2.0

# 参数搜索（可选）
optuna>=3.0.0

